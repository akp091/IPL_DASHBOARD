import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IPL T20 Live Dashboard - Live Cricket Updates & Statistics",
  description:
    "Real-time IPL T20 match information, live scores, points table, and match schedule. Get live cricket updates and statistics.",
  keywords:
    "IPL, T20, cricket, live scores, points table, match schedule, live updates",
  authors: [{ name: "IPL Dashboard Team" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
